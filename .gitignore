node_modules/
.devcontainer/
dist/
.env
.DS_Store
.vscode-test/
*.vsix
/test-workspace/
tmp/

# pnpm content-addressable store (created in-repo when the global store is on a
# different filesystem); never commit it — pnpm-lock.yaml is the source of truth.
.pnpm-store/

.claude/worktrees/
scheduled_tasks.lock

/node_modules
.codex/environments/environment.toml
