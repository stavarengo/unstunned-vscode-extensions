# Run pre/post lifecycle scripts (e.g. `pretest`) like npm does.
# The unit-test harness relies on `pretest` (tsc emit) producing dist/test/**/*.js
# before mocha runs; pnpm does not run pre/post scripts by default.
enable-pre-post-scripts=true
